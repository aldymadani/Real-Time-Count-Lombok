<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.realtimecountlombok">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RealTimeCountLombok.Light">
        <activity android:name=".general.RegisterActivity"></activity>
        <activity android:name=".admin.EditSuaraActivity" />
        <activity android:name=".admin.MainManageSuaraActivity" />
        <activity android:name=".general.TotalSuaraKecamatanActivity" />
        <activity android:name=".general.TotalSuaraActivity" />
        <activity android:name=".InputSuaraActivity" />
        <activity android:name=".general.LoginActivity" />
        <activity android:name=".general.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>